{
  "name": "@tva-controle/connector-pennylane",
  "version": "0.1.0",
  "description": "Connecteur Pennylane (Company API v2) — normalise les réponses vers le format pivot commun du système de contrôle/calcul TVA.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@tva-controle/core": "*"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "@types/node": "^22.5.0"
  }
}
